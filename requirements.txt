# requirements.txt
# COMP702 MSc dissertation — weekly ML habitat-suitability models for Culex nigripalpus, Florida
# Derived from an AST audit of all 46 project scripts.
#
# NOTE ON VERSIONS: the version floors below are the minimum releases that support the
# APIs actually used in this codebase. 

# --- Core data handling ---
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0          # Parquet engine; required by the 37 scripts using read_parquet/to_parquet

# --- Modelling ---
scikit-learn>=1.3.0      # RandomForestClassifier, IsotonicRegression, GroupKFold,
                         # LeaveOneGroupOut, GroupShuffleSplit, KMeans, metrics
xgboost>=2.0.0           # XGBClassifier, XGBModel, DMatrix, tree_method="hist"
elapid>=1.0.0            # MaxentModel (linear/quadratic/hinge features, cloglog transform)

# --- Statistics ---
scipy>=1.10.0            # cKDTree, spearmanr, hierarchical linkage/dendrogram, squareform

# --- Interpretability ---
shap>=0.44.0             # TreeExplainer / KernelExplainer for per-model SHAP attribution

# --- Figures and animations ---
matplotlib>=3.7.0        # Agg backend, GridSpec, Normalize, Line2D
imageio>=2.31.0          # imageio.v2 — animated GIF export for weekly suitability surfaces

# --- Remote sensing / data acquisition (extraction stage only) ---
earthengine-api>=0.1.380 # imported as `ee` — MODIS, Daymet, land-cover extraction
geemap>=0.30.0           # Earth Engine export helpers in extract_data_loop.py

# --- Standard library (no install required; listed for completeness only) ---
# argparse, datetime, gc, glob, itertools, json, os, pathlib, re, typing, __future__

# --- First-party project modules (no install required) ---
# baselines, cv_harness, derived_maps, map_style, maxent_common, nowcast_cv,
# report_style, shap_common2, shaps_common